# 🔍 OCR Платформа для документов

Система распознавания документов о переподготовке и повышении квалификации с интерактивной разметкой полей для новых типов документов.

## 📋 Поддерживаемые организации и документы

### 🏢 **1Т**
- ✅ Удостоверения о повышении квалификации
- ✅ Дипломы о профессиональной переподготовке

### 🏛️ **РОСНОУ (Российский новый университет)**
- ✅ Удостоверения о повышении квалификации
- ✅ Дипломы о профессиональной переподготовке

### 🏦 **Финансовый университет**
- ✅ Удостоверения (вариант 1) - ФИО в одну строку
- ✅ Удостоверения (вариант 2) - ФИО на трёх строках в дательном падеже ⚠️

## 🔧 Извлекаемые поля

- **ФИО** (в именительном падеже)
- **Серия и номер** документа
- **Регистрационный номер**
- **Дата выдачи** (в ISO формате)

## 🚀 Установка и запуск

### Системные требования

- **Python 3.8+**
- **Tesseract OCR** (см. инструкции по установке ниже)

### 1. Клонирование проекта

```

git clone <repository-url>
cd pdf_ocr_platform

```

### 2. Установка зависимостей

```

pip install -r requirements.txt

```

### 3. Установка Tesseract OCR

#### Windows
1. Скачайте и установите с [GitHub](https://github.com/UB-Mannheim/tesseract/wiki)
2. Добавьте путь к tesseract.exe в переменную PATH или укажите при запуске

#### Ubuntu/Debian
```

sudo apt update
sudo apt install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng

```

#### MacOS
```

brew install tesseract tesseract-lang

```

### 4. Запуск приложения

#### Базовый запуск
```

python app.py

```

#### С дополнительными параметрами
```


# Запуск на другом порту

python app.py --port 8080

# Запуск с внешним доступом

python app.py --host 0.0.0.0 --port 80

# Указание пути к Tesseract

python app.py --tesseract-path "C:\Program Files\Tesseract-OCR\tesseract.exe"

# Режим отладки

python app.py --debug

```

#### Проверка системы
```


# Проверить зависимости без запуска

python app.py --check-only

# Создать файл requirements.txt

python app.py --create-requirements

```

## 🏗️ Структура проекта

```

pdf_ocr_platform/
├── app.py                    \# Главное приложение (точка входа)
├── requirements.txt          \# Зависимости Python
├── README.md                \# Документация
├──
├── core/                    \# Основные модули OCR
│   ├── ocr_engine.py        \# OCR движок с Tesseract
│   ├── config.py            \# Конфигурации и парсеры документов
│   └── __init__.py
├──
├── web/                     \# Веб-интерфейс
│   ├── dashboard.py         \# Основной Dash интерфейс
│   ├── markup_tool.py       \# Интерактивная разметка полей
│   └── __init__.py
├──
├── static/                  \# Статические файлы (создается автоматически)
└── ocr_platform.log        \# Лог файл (создается автоматически)

```

## 💻 Использование

### Основной режим распознавания

1. Откройте браузер и перейдите на `http://127.0.0.1:8050`
2. На вкладке **"📄 Распознавание документов"**:
   - Загрузите PDF файл
   - Выберите тип документа из списка
   - При необходимости поверните изображение
   - Нажмите **"🚀 Запустить OCR"**
3. Просмотрите результаты и экспортируйте в CSV/JSON

### Режим разметки новых документов

1. Перейдите на вкладку **"🎯 Разметка полей"**
2. Загрузите изображение документа через основную вкладку
3. Выберите тип поля для разметки
4. Выделите область поля мышью на изображении
5. Повторите для всех полей
6. Сохраните конфигурацию в JSON формате

### Интеграция новых конфигураций

Для добавления размеченной конфигурации в систему:

1. Скопируйте JSON из инструмента разметки
2. Добавьте парсеры в `core/config.py`:

```


# В DOCUMENT_CONFIGS добавьте новую конфигурацию

'MY_ORG_CERTIFICATE': DocumentConfig(
name="Моя организация Удостоверение",
organization="MY_ORG",
document_type="certificate",
fields=fields_from_markup,  \# из JSON разметки
patterns={
'fullname': CommonParsers.parse_fullname,
'seriesandnumber': CommonParsers.parse_series_number,
\# ... другие парсеры
}
)

```

## 🔧 Настройка OCR

### Параметры для разных типов полей

Система автоматически применяет оптимизированные настройки:

- **ФИО**: масштаб x3, контраст +50%
- **Серия и номер**: масштаб x4, контраст +100%, двуязычное распознавание
- **Регистрационный номер**: масштаб x4, контраст +100%
- **Дата**: масштаб x3, контраст +80%

### Специальные возможности

- **Удаление линий** для документов ФинУниверситета
- **Коррекция OCR-ошибок** для каждого типа документа
- **Система неуверенности** для пометки сомнительных результатов

## 🐛 Решение проблем

### Tesseract не найден

```


# Установите переменную окружения

export TESSERACT_PATH=/usr/bin/tesseract

# Или укажите при запуске

python app.py --tesseract-path /path/to/tesseract

```

### Ошибки зависимостей

```


# Переустановите зависимости

pip install --upgrade -r requirements.txt

# Проверьте версию Python

python --version  \# Должен быть >= 3.8

```

### Проблемы с качеством распознавания

1. Убедитесь, что PDF в хорошем качестве
2. Попробуйте повернуть изображение
3. Проверьте правильность выбора конфигурации
4. Для новых документов используйте разметку полей

### Ошибки веб-интерфейса

- Проверьте, что порт не занят другим приложением
- Попробуйте запустить на другом порту: `python app.py --port 8080`
- Очистите кэш браузера

## 🚀 Преимущества над Streamlit версией

- ✅ **Полная поддержка OpenCV** без ограничений
- ✅ **Лучшая интерактивность** с Plotly и Dash
- ✅ **Стабильный деплой** без проблем Streamlit
- ✅ **Интерактивная разметка** для новых типов документов
- ✅ **Гибкая архитектура** для расширения функциональности

## 📊 API для интеграции

Приложение можно интегрировать в другие системы:

```

from web.dashboard import create_dash_app
from core.ocr_engine import DocumentProcessor
from core.config import get_config

# Создание Dash приложения

app = create_dash_app(tesseract_cmd="/path/to/tesseract")

# Прямое использование OCR

processor = DocumentProcessor(tesseract_cmd="/path/to/tesseract")
config = get_config('1T_CERTIFICATE')
results = processor.extract_fields(image, config.__dict__)

```

При возникновении проблем:

1. Проверьте файл логов: `ocr_platform.log`
2. Запустите диагностику: `python app.py --check-only`
3. Убедитесь, что все зависимости установлены правильно